#include "main.h"
#include <stdio.h>

/**
 * _puts_recursion - prints a string
 * @s: character
 * return: 0
 */
void _puts_recursion(char *s)
{
	char n;
	{
	putchar("%s");
	}
	putchar('\n');

	return (0);
}

