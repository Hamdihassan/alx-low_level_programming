#include "main.h"
#include <stdio.h>

/**
 * main - adds positive numbers
 * @argc: argumrny count
 * @argv: argument vector
 * Return: Nothing
 */

int main(int argc, char *argv[])

{
	int i;
	int j;
	int sum;
	sum = 0;
	if (i < 0)
	{
		printf("%d\n", sum);
	return (0);
		sum += atoi(argv[i]);
	}
	else 
	{
		printf("Error\n");
		return (1);
	}
	i++;
}

