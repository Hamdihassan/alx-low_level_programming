#include <stdio.h>
/**
* main - c program that prints alphabets in lowercase
*Return: 0
*/

int main(void)
{
char ch;
for (char = 'a'; ch <= 'z'; char++)
{
putchar(ch);
}
putchar('\n');
return (0);
}
