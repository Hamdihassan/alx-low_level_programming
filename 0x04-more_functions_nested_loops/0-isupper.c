#include "main.h"

/**
 * _isupper - check the code.
 *@x:x is integer
 * Return: 1 if c is uppercase, otherwise 0.
 */
int _isupper(int x)
{
	if (x >= 65 && x <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
